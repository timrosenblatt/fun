# https://taskfile.dev

version: '3'

vars:
  GREETING: Hello, World!

tasks:
  default:
    cmds:
      - echo "{{.GREETING}}"
    silent: true
  create_cluster:
    cmds:
      - need to create the cluster
      - need to install prometheus
      - "? maybe install grafana?"
  teardown_cluster:
    cmds:
  make_binary:
    cmds:
  make_dockerimage:
    cmds:
